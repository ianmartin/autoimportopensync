INCLUDE_DIRECTORIES( ${OPENSYNC_INCLUDE_DIR} ${OPENOBEX_INCLUDE_DIR} ${GLIB2_INCLUDE_DIRS} )

# bfb source	
SET( bfb_LIB_SRCS
bfb.c
bfb_io.c
crc.c
)

ADD_LIBRARY( bfb STATIC ${bfb_LIB_SRCS} )

TARGET_LINK_LIBRARIES( bfb ${OPENOBEX_LIBRARIES} ${GLIB2_LIBRARIES} )

