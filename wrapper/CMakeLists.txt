INCLUDE_DIRECTORIES( ${PYTHON_INCLUDE_PATH} )

INCLUDE( UseSWIG )

SET( OPENSYNC_WRAPPER_SRC opensync.i )

IF ( PYTHON_LIBRARY )
	SWIG_ADD_MODULE( opensync python ${OPENSYNC_WRAPPER_SRC} )
	SWIG_LINK_LIBRARIES( opensync ${OPENSYNC_LIBRARY} )	

	SET( OPENSYNC_PYTHON_WRAPPER ${CMAKE_CURRENT_BINARY_DIR}/_opensync.so ${CMAKE_CURRENT_BINARY_DIR}/opensync.py  )

	INSTALL( FILES ${OPENSYNC_PYTHON_WRAPPER} DESTINATION ${LIB_INSTALL_DIR}/python/site-packages/ )

ENDIF ( PYTHON_LIBRARY  )	

