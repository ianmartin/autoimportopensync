INCLUDE_DIRECTORIES( ${OPENSYNC_INCLUDE_DIR} ${GLIB2_INCLUDE_DIRS} ${LIBXML2_INCLUDE_DIR} )

OPENSYNC_PLUGIN_ADD( file-sync file_sync.c )

TARGET_LINK_LIBRARIES( file-sync ${OPENSYNC_LIBRARY} )

###### INSTALL ################### 
OPENSYNC_PLUGIN_INSTALL( file-sync ) 

OPENSYNC_PLUGIN_CONFIG( file-sync )
