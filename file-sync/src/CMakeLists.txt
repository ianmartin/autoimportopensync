INCLUDE_DIRECTORIES( ${OPENSYNC_INCLUDE_DIR} ${GLIB2_INCLUDE_DIRS} )

ADD_LIBRARY( file-sync SHARED file_sync.c )

TARGET_LINK_LIBRARIES( file-sync ${OPENSYNC_LIBRARY} )

###### INSTALL ################### 
INSTALL( TARGETS file-sync DESTINATION ${OPENSYNC_PLUGINDIR} ) 

INSTALL( FILES "${CMAKE_CURRENT_SOURCE_DIR}/file-sync" DESTINATION ${OPENSYNC_CONFIGDIR} )
