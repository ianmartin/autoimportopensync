INCLUDE_DIRECTORIES( ${OPENSYNC_INCLUDE_DIR} ${GLIB2_INCLUDE_DIRS} ${LIBXML2_INCLUDE_DIR} ${LIBSYNCML_INCLUDE_DIR} )

ADD_LIBRARY( syncml-plugin SHARED syncml_plugin.c )

TARGET_LINK_LIBRARIES( syncml-plugin ${OPENSYNC_LIBRARIES} ${LIBSYNCML_LIBRARIES} )

###### INSTALL ################### 
INSTALL( TARGETS syncml-plugin DESTINATION ${OPENSYNC_PLUGINDIR} ) 

SET( DEFAULT_CONFIGS 
  ${CMAKE_CURRENT_SOURCE_DIR}/syncml-obex-client
  ${CMAKE_CURRENT_SOURCE_DIR}/syncml-http-server
)

INSTALL( FILES ${DEFAULT_CONFIGS} DESTINATION ${OPENSYNC_CONFIGDIR} )
