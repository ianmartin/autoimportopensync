INCLUDE_DIRECTORIES( ${OPENSYNC_INCLUDE_DIR} ${GLIB2_INCLUDE_DIRS} ${LIBXML2_INCLUDE_DIR} ${SYNCML_INCLUDE_DIR} )

ADD_LIBRARY( syncml-sync SHARED syncml_plugin.c )

TARGET_LINK_LIBRARIES( syncml-sync ${OPENSYNC_LIBRARY} )

###### INSTALL ################### 
INSTALL( TARGETS syncml-sync DESTINATION ${OPENSYNC_PLUGINDIR} ) 

INSTALL( FILES "${CMAKE_CURRENT_SOURCE_DIR}/syncml-sync" DESTINATION ${OPENSYNC_CONFIGDIR} )
