Summary:		IrMC plugin for OpenSync
Name:			@PACKAGE@
Version:		@VERSION@
Release:		1
License:		GPL
Group:			Development/Libraries
Source:			%{name}-%{version}.tar.gz
BuildRoot:		%{_tmppath}/%{name}-%{version}
Requires:		glib2 libopensync libxml2 bluez-libs openobex
BuildRequires:	glib2-devel libopensync-devel libxml2 bluez-libs-devel openobex-devel

%description
This plugin allows applications using OpenSync to synchronise to and from
devices that use the IrMC protocol. The plugin supports both IrDA and Bluetooth.

%prep
%setup -q

%build
%configure --enable-obex
make

%install
rm -rf %{buildroot}
make install DESTDIR=%{buildroot}
rm -f %{buildroot}%{_libdir}/opensync/plugins/*.la

%clean
rm -rf %{buildroot}

%files
%defattr(-,root,root)
%{_libdir}/opensync/plugins/irmc_sync.so
%{_datadir}/opensync/defaults/irmc-sync

%changelog
* Tue Oct  4 2005 Pierre Ossman <drzeus@drzeus.cx> 0.01-1
- Initial package
