LINK_DIRECTORIES( ${OPENSYNC_LIBRARY_DIRS} ${GLIB2_LIBRARY_DIRS} ${LIBXML2_LIBRARY_DIRS} ${LIBUUID_LIBRARY_DIRS} )
INCLUDE_DIRECTORIES( ${OPENSYNC_INCLUDE_DIRS} ${GLIB2_INCLUDE_DIRS} ${LIBXML2_INCLUDE_DIRS} ${LIBUUID_INCLUDE_DIRS} )

OPENSYNC_PLUGIN_ADD( tomboy-sync tomboy_sync.c )

TARGET_LINK_LIBRARIES( tomboy-sync ${OPENSYNC_LIBRARIES} ${GLIB2_LIBRARIES} ${LIBXML2_LIBRARIES} ${LIBUUID_LIBRARIES} )

###### INSTALL ################### 
OPENSYNC_PLUGIN_INSTALL( tomboy-sync ) 

OPENSYNC_PLUGIN_CONFIG( tomboy-sync )
