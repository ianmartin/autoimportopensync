LINK_DIRECTORIES( ${OPENSYNC_LIBRARY_DIRS} ${GLIB2_LIBRARY_DIRS} )
INCLUDE_DIRECTORIES( ${OPENSYNC_INCLUDE_DIRS} ${GLIB2_INCLUDE_DIRS} )

SET( vconvert_SRCS
vconvert.c
)

ADD_EXECUTABLE( vconvert ${vconvert_SRCS} )
TARGET_LINK_LIBRARIES( vconvert ${OPENSYNC_LIBRARIES} ${GLIB2_LIBRARIES} )

###### INSTALL ################### 

INSTALL( TARGETS vconvert DESTINATION ${BIN_INSTALL_DIR} ) 

