INCLUDE_DIRECTORIES( ${LIBEBOOK_INCLUDE_DIR} ${LIBECAL_INCLUDE_DIR} ${LIBEDATABOOK_INCLUDE_DIR} ${LIBEDATACAL_INCLUDE_DIR} ${LIBEDATASERVER_INCLUDE_DIR} ${GCONF2_INCLUDE_DIR} ${OPENSYNC_INCLUDE_DIR} ${GLIB2_INCLUDE_DIRS} ${LIBXML2_INCLUDE_DIR} )

SET( evo2_sync_LIB_SRCS
  evolution2_sync.c 
  evolution2_ebook.c
  evolution2_ecal.c
  evolution2_etodo.c
)

OPENSYNC_PLUGIN_ADD( evo2-sync ${evo2_sync_LIB_SRCS} ) 

TARGET_LINK_LIBRARIES( evo2-sync ${LIBEBOOK_LIBRARIES} ${LIBECAL_LIBRARIES} ${LIBEDATABOOK_LIBRARIES} ${LIBEDATACAL_LIBRARIES} ${LIBEDATASERVER_LIBRARIES} ${OPENSYNC_LIBRARY} )

###### INSTALL ################### 
OPENSYNC_PLUGIN_INSTALL( evo2-sync )

OPENSYNC_PLUGIN_CONFIG( evo2-sync )
